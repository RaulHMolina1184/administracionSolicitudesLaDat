@page
@model administracionSolicitudesLaDat.Pages.IndexModel
@{
    ViewData["Title"] = "Iniciar sesión";
    Layout = "_LoginLayout"; // Carga el layout de login
}

<div class="container d-flex align-items-center justify-content-center min-vh-100">
    <div class="card shadow-lg p-4" style="max-width: 400px; width: 100%; background-color: #e6e6e6;">
        <div class="text-center">
            <img src="/images/logo1.png" alt="Logo" class="img-fluid mb-3" style="max-height: 150px;" />
            <h4 class="card-title mb-3">Iniciar sesión</h4>
        </div>

        <form method="post">
            @Html.AntiForgeryToken()
            <div class="form-group mb-3">
                <label asp-for="NombreUsuario" class="form-label">Usuario</label>
                <input type="text" asp-for="NombreUsuario" name="NombreUsuario" class="form-control"
                       placeholder="Ingrese su usuario" required />
                <span asp-validation-for="NombreUsuario" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label asp-for="Contrasenia" class="form-label">Contraseña</label>
                <input type="password" asp-for="Contrasenia" name="Contrasenia" class="form-control"
                       placeholder="Ingrese su contraseña" required />
                <span asp-validation-for="Contrasenia" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary w-100">Aceptar</button>
        </form>

        @if (!string.IsNullOrEmpty(Model.Mensaje))
        {
            <div class="alert alert-danger mt-3" role="alert">
                @Model.Mensaje
            </div>
        }

        @if (!string.IsNullOrEmpty(Model.MensajeInfo))
        {
            <div class="alert alert-info mt-3" role="alert">
                @Model.MensajeInfo
            </div>
        }
    </div>
</div>
